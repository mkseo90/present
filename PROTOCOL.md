# POV Wand — BLE 통신 프로토콜

버전: v0.1 (2026-07-29) · 대상 HW: Seeed XIAO nRF52840 Sense

앱(Web Bluetooth 웹앱)과 기기(POV Wand) 사이의 통신 규약.
기능명세서 v2의 F6(BLE 문구 변경), F7(콘텐츠 슬롯), F8(앱 설정)을 구현한다.

---

## 1. 전송 계층

Nordic UART Service(NUS) 사용. BLE 상에 양방향 시리얼 통로를 만든다.

| 항목 | 값 |
|------|-----|
| 광고 이름 | `POV-STICK` |
| Service UUID | `6E400001-B5A3-F393-E0A9-E50E24DCCA9E` |
| RX Characteristic (앱→기기, Write) | `6E400002-B5A3-F393-E0A9-E50E24DCCA9E` |
| TX Characteristic (기기→앱, Notify) | `6E400003-B5A3-F393-E0A9-E50E24DCCA9E` |

- 펌웨어: Bluefruit 라이브러리 `BLEUart` 사용, MTU 247 협상 (`Bluefruit.configPrphConn(247)`)
- 앱: Web Bluetooth. 한 번의 write는 512바이트 이하로 유지
- 페어링/본딩 없음 (장난감이므로 Just Works, 암호화 생략)

## 2. 프레이밍

- **UTF-8 텍스트, 줄바꿈(`\n`) 종료.** 한 줄 = 한 명령/응답
- 한 줄 최대 240바이트 (MTU 안에 들어가도록; 초과분은 이미지 청크 방식 사용)
- 토큰 구분은 스페이스 1개. 마지막 인자(텍스트 등)는 스페이스 포함 가능
- 시리얼 BLE 터미널 앱(nRF Connect 등)으로 수동 테스트 가능한 것이 설계 목표

## 3. 명령 (앱 → 기기)

### 3.1 표시

| 명령 | 동작 |
|------|------|
| `SHOW <색> <텍스트>` | 즉시 표시. 플래시에 저장하지 않음. 재부팅하면 사라짐 |
| `SHOWSLOT <n>` | 슬롯 n의 콘텐츠를 즉시 표시 (= SEL과 동일하되 저장된 선택을 바꾸지 않음) |

`SHOW`는 송별회 실시간 연출용. 현재 표시 중인 콘텐츠를 덮어쓴다.

### 3.2 슬롯 저장 (자녀용 콘텐츠, 기기 단독 동작의 기반)

| 명령 | 동작 |
|------|------|
| `SAVE <슬롯> <색> <텍스트>` | 텍스트를 슬롯에 저장 |
| `IMGB <슬롯> <컬럼수>` | 이미지 전송 시작 선언 (높이는 8픽셀 고정) |
| `IMGD <순번> <base64>` | 이미지 데이터 청크. 순번은 0부터 |
| `IMGE <CRC32>` | 전송 종료. CRC32(원본 바이너리, hex 8자리) 일치 시에만 저장 |
| `SEL <슬롯>` | 부팅 시 표시할 슬롯 선택 (버튼 순환의 시작점) |
| `DEL <슬롯>` | 슬롯 삭제 |
| `LIST` | 슬롯 목록 조회 |

- 슬롯 번호: 1~12 (0은 즉시 표시 버퍼로 예약)
- 이미지 포맷: 컬럼 우선, 컬럼당 8픽셀 × RGB 3바이트 = 24바이트. 예) 24컬럼 = 576바이트
- `IMGB` 후 `IMGD` 순번이 어긋나면 `ERR 5`, 처음부터 재전송

### 3.3 설정

| 명령 | 동작 |
|------|------|
| `SET BRIGHT <0~100>` | 밝기(%) |
| `SET MODE <auto\|pov\|idle>` | auto: 스윙 감지로 POV/대기 자동 전환(기본), pov/idle: 강제 |
| `SET SPEED <ms>` | 컬럼 간격 수동 오버라이드 (0 = 가속도센서 자동) |

설정은 플래시에 저장되어 재부팅 후 유지된다.

### 3.4 조회

| 명령 | 응답 예 |
|------|---------|
| `PING` | `PONG` |
| `DFU <PIN>` | (v0.0.2에서 보류 — 실기기 검증 후 재도입 예정) |
| `INFO` | `INFO fw=0.1 slots=12 used=5 bat=87 mode=auto bright=80 owner=주인공 serial=No.001/001 mac=C4:F3:...` |

## 3.5 하드웨어 브링업 (앱은 사용하지 않음)

실기기 조립·수리 시 배선을 확인하는 진단 명령. 정규 기능이 아니라 개발용이며,
앱은 이 명령을 보내지 않는다. LED_TYPE 3(GPIO 직결) 전용.

| 명령 | 동작 |
|------|------|
| `TEST on` | 7채널 전부 점등(홀드) |
| `TEST off` | 7채널 전부 소등(홀드) |
| `TEST chase` | 1개씩 순차 점등. 전류가 1/7이라 전원 여유 확인에 유용 |
| `TEST pin <n>` | n번 채널만 계속 점등. 채널별 배선 확인·프로빙용 |
| `TEST drive low\|high` | GPIO 구동 세기 (S0S1 약 2mA / H0H1 약 9mA) |
| `TEST end` | 홀드 해제, 일반 표시로 복귀 |

**주의**: `SET BRIGHT`는 LED_TYPE 3에서 **아무 효과가 없다.** 단색 직결 LED는
`digitalWrite` 켜짐/꺼짐뿐이라 밝기 개념이 없다. 실질적인 밝기 수단은 `TEST drive`다.

### PROBE — 멀티미터 없이 하는 배선 진단

핀을 내부 풀 저항(약 13kΩ) 입력으로 바꾼 뒤 SAADC로 패드 전압을 읽는다.
`analogRead()`는 `PIN_CNF`를 건드리지 않고 `RESP_Bypass`로 읽으므로 풀 설정이 유지된다.

| 명령 | 동작 |
|------|------|
| `PROBE up` (기본) | 각 LED 핀을 풀업 입력으로 두고 전압 회신 |
| `PROBE down` | 각 LED 핀을 풀다운 입력으로 두고 전압 회신 |
| `PROBE sweep` | D0~D10 + 내장 LED 핀까지 전체 훑기 (풀업/풀다운 양쪽) |

응답 형식: `PROBE ch<n> D<pin> <mV>` 반복 후 `OK PROBE <up|down>`
(`sweep`은 `SW D<pin> up=<mV> dn=<mV>` 형식, ADC 없는 핀은 `H`/`L` 디지털값)

**판독표** (VDD 3.3V, 내부 풀 13kΩ 기준 실측값):

| 풀업 | 풀다운 | 의미 |
|------|--------|------|
| ~3300mV | ~0mV | **개방 — 아무것도 연결되지 않음 (단선)** |
| ~3300mV | ~2440mV | 저항성 풀업(약 4.7kΩ) — 확장보드 I2C 등 |
| ~3300mV | ~650mV | **다이오드성 부하 = LED가 실제로 연결됨** |
| ~1800~2100mV | ~0mV | LED가 GND 쪽으로 연결됨 |
| ~0mV | ~0mV | GND로 쇼트 |

`~650mV`는 보드 내장 LED(D13, 애노드→3.3V·캐소드→GPIO)에서 측정한 값이라
**양성 대조군**으로 쓸 수 있다. 전체 채널이 "개방"으로 나오면 개별 배선보다
**공통 GND 단선**을 먼저 의심할 것 (2026-08-19 실제 사례).

ADC 사용 가능 핀은 AIN0~AIN7 = P0.02/03/04/05/28/29/30/31, 즉 **D0~D5와 D13**뿐이다.
D6~D10(P1.11~P1.15)은 ADC가 없어 디지털 판독만 가능하다.

## 4. 응답 (기기 → 앱)

- 성공: `OK <명령이름>` (예: `OK SAVE`)
- 실패: `ERR <코드> <사유>` (예: `ERR 2 slot out of range`)
- `LIST` 응답: 줄 단위 `SLOT <n> <TXT|IMG> <미리보기>` 반복 후 `OK LIST`

```
SLOT 1 TXT 사랑해
SLOT 2 IMG 24col
SLOT 3 TXT rainbow:공주님
OK LIST
```

### 오류 코드

| 코드 | 의미 |
|------|------|
| 1 | 알 수 없는 명령 |
| 2 | 인자 범위 초과 (슬롯 번호 등) |
| 3 | 저장 공간 부족 |
| 4 | CRC 불일치 |
| 5 | 이미지 청크 순번 오류 |
| 6 | 텍스트에 폰트 없는 문자 포함 (해당 문자는 □로 대체 후 경고) |
| 7 | DFU PIN 불일치 |

## 5. 색 지정

| 형식 | 예 | 의미 |
|------|-----|------|
| `RRGGBB` | `FF66CC` | 단색 (hex) |
| `rainbow` | | 글자/컬럼 진행에 따라 무지개 순환 |
| `grad:RRGGBB-RRGGBB` | `grad:FF0000-0000FF` | 시작→끝 그라데이션 |

## 6. 이미지 전송 예시 (8×24 하트, 576바이트)

```
앱 → IMGB 5 24
기기 → OK IMGB
앱 → IMGD 0 <base64 180바이트 분량>
앱 → IMGD 1 <base64 180바이트 분량>
앱 → IMGD 2 <base64 180바이트 분량>
앱 → IMGD 3 <base64 나머지>
기기 → (청크마다 응답 없음 — 흐름 단순화)
앱 → IMGE 8F3A21BC
기기 → OK IMGE        ← 이 시점에 플래시 기록 완료
```

## 7. 이벤트 (기기 → 앱, 선택 구현)

연결 중일 때만 push. 앱은 없어도 동작에 지장 없음.

| 이벤트 | 의미 |
|--------|------|
| `EV SWING <주기ms>` | 스윙 감지됨 (앱에서 "흔드는 중" 표시용) |
| `EV SLEEP` / `EV WAKE` | 슬립 진입/기상 |
| `EV BAT <퍼센트>` | 배터리 변화 (10% 단위) |

## 부록: 펌웨어 버전 규칙

`INFO`의 `fw=` 값(semver)으로 앱이 UI를 전환한다:
- **1.0.0 미만** = 단색 완드 → 앱이 색 선택 UI를 숨기고 `color=`가 알려준 색으로 미리보기
- **1.0.0 이상** = RGB 완드 → 풀컬러 UI (색상·무지개·그라데이션 선택 노출)
- 버전 판별 불가(시뮬레이터 `fw=sim` 등) → **단색** (현재 존재하는 하드웨어가 전부 단색이라,
  못 쓰는 색 선택 UI를 보여주는 편이 더 혼란스럽다)

앱은 연결 전·연결 해제 후에도 단색 UI를 유지한다. 3색 LED 완드를 만들 때
그 펌웨어의 `FW_VERSION`을 `1.0.0` 이상으로 올리면 앱이 자동으로 풀컬러 UI로 전환된다.

### 보드별 하드웨어 사양 (`color`, `leds`)

보드마다 LED 색과 개수가 다를 수 있으므로 기기가 `INFO`로 알려주고 앱이 그대로 따른다.

| 필드 | 예 | 용도 |
|------|-----|------|
| `color=RRGGBB` | `color=00FF66` | 그 보드에 실제로 달린 LED 색. 앱 미리보기 색·라벨에 사용 |
| `leds=<n>` | `leds=7` | LED 개수. 앱 미리보기의 행 수에 사용 |

`color`는 펌웨어의 `secrets.h` `OWNERS[]` 테이블에 MAC과 함께 적어둔다
(단색 완드는 펌웨어가 색을 쓰지 않으므로, 이 값은 순수하게 앱 표시용이다).
미등록 보드는 `color=-`로 응답하고 앱이 기본값으로 대체한다.

현재: 선물용 단색(GREEN) 완드 펌웨어 = 0.0.2. OTA(BLE DFU)는 보류 상태 —
유선 DFU(`adafruit-nrfutil`)로 업데이트한다 (HANDOFF.md 참고).

## 부록: 주인 지정

펌웨어의 `OWNERS[]` 테이블(`secrets.h`, gitignore됨)에 보드 MAC 주소를 등록하면
그 보드의 주인 이름·시리얼·LED 색이 정해진다. MAC은 부팅 시 시리얼 모니터에
`MAC: AA:BB:CC:DD:EE:FF` 형태로 출력된다.

**MAC은 전체를 적지 않아도 된다 — 뒷부분만 적으면 그것으로 일치를 판정한다**(최소 2글자).
콜론 단위로 끊는 것을 권장한다 (시리얼 출력에서 그대로 잘라 붙일 수 있고 오타가 적다).

```c
struct Owner { const char* mac; const char* name; const char* serial; const char* color; };
static const Owner OWNERS[] = {
  { "DD:EE:FF", "이름", "No.001/001", "00FF66" },   // 뒤 3옥텟 + GREEN LED
};
```

등록된 보드는 부팅 크레딧으로 `<이름>의 선물 <시리얼>`을 표시하고,
`INFO` 응답에 `owner=`/`serial=`/`mac=`/`color=`가 포함된다.
이름에는 공백을 넣지 않는다 (INFO가 공백 구분이므로).

## 변경 이력

- v0.1 (2026-07-29): 최초 작성. INFO에 owner/serial/mac 필드 추가
