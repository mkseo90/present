# POV Wand — BLE 통신 프로토콜

버전: v0.1 (2026-07-29) · 대상 HW: Seeed XIAO nRF52840 Sense

앱(Web Bluetooth 웹앱)과 기기(POV Wand) 사이의 통신 규약.
기능명세서 v2의 F6(BLE 문구 변경), F7(콘텐츠 슬롯), F8(앱 설정)을 구현한다.

---

## 1. 전송 계층

Nordic UART Service(NUS) 사용. BLE 상에 양방향 시리얼 통로를 만든다.

| 항목 | 값 |
|------|-----|
| 광고 이름 | `POV-STICK` |
| Service UUID | `6E400001-B5A3-F393-E0A9-E50E24DCCA9E` |
| RX Characteristic (앱→기기, Write) | `6E400002-B5A3-F393-E0A9-E50E24DCCA9E` |
| TX Characteristic (기기→앱, Notify) | `6E400003-B5A3-F393-E0A9-E50E24DCCA9E` |

- 펌웨어: Bluefruit 라이브러리 `BLEUart` 사용, MTU 247 협상 (`Bluefruit.configPrphConn(247)`)
- 앱: Web Bluetooth. 한 번의 write는 512바이트 이하로 유지
- 페어링/본딩 없음 (장난감이므로 Just Works, 암호화 생략)

## 2. 프레이밍

- **UTF-8 텍스트, 줄바꿈(`\n`) 종료.** 한 줄 = 한 명령/응답
- 한 줄 최대 240바이트 (MTU 안에 들어가도록; 초과분은 이미지 청크 방식 사용)
- 토큰 구분은 스페이스 1개. 마지막 인자(텍스트 등)는 스페이스 포함 가능
- 시리얼 BLE 터미널 앱(nRF Connect 등)으로 수동 테스트 가능한 것이 설계 목표

## 3. 명령 (앱 → 기기)

### 3.1 표시

| 명령 | 동작 |
|------|------|
| `SHOW <색> <텍스트>` | 즉시 표시. 플래시에 저장하지 않음. 재부팅하면 사라짐 |
| `SHOWSLOT <n>` | 슬롯 n의 콘텐츠를 즉시 표시 (= SEL과 동일하되 저장된 선택을 바꾸지 않음) |

`SHOW`는 송별회 실시간 연출용. 현재 표시 중인 콘텐츠를 덮어쓴다.

### 3.2 슬롯 저장 (자녀용 콘텐츠, 기기 단독 동작의 기반)

| 명령 | 동작 |
|------|------|
| `SAVE <슬롯> <색> <텍스트>` | 텍스트를 슬롯에 저장 |
| `IMGB <슬롯> <컬럼수>` | 이미지 전송 시작 선언 (높이는 8픽셀 고정) |
| `IMGD <순번> <base64>` | 이미지 데이터 청크. 순번은 0부터 |
| `IMGE <CRC32>` | 전송 종료. CRC32(원본 바이너리, hex 8자리) 일치 시에만 저장 |
| `SEL <슬롯>` | 부팅 시 표시할 슬롯 선택 (버튼 순환의 시작점) |
| `DEL <슬롯>` | 슬롯 삭제 |
| `LIST` | 슬롯 목록 조회 |

- 슬롯 번호: 1~12 (0은 즉시 표시 버퍼로 예약)
- 이미지 포맷: 컬럼 우선, 컬럼당 8픽셀 × RGB 3바이트 = 24바이트. 예) 24컬럼 = 576바이트
- `IMGB` 후 `IMGD` 순번이 어긋나면 `ERR 5`, 처음부터 재전송

### 3.3 설정

| 명령 | 동작 |
|------|------|
| `SET BRIGHT <0~100>` | 밝기(%) |
| `SET MODE <auto\|pov\|idle>` | auto: 스윙 감지로 POV/대기 자동 전환(기본), pov/idle: 강제 |
| `SET SPEED <ms>` | 컬럼 간격 수동 오버라이드 (0 = 가속도센서 자동) |

설정은 플래시에 저장되어 재부팅 후 유지된다.

### 3.4 조회

| 명령 | 응답 예 |
|------|---------|
| `PING` | `PONG` |
| `INFO` | `INFO fw=0.1 slots=12 used=5 bat=87 mode=auto bright=80 owner=주인공 serial=No.001/001 mac=C4:F3:...` |

## 4. 응답 (기기 → 앱)

- 성공: `OK <명령이름>` (예: `OK SAVE`)
- 실패: `ERR <코드> <사유>` (예: `ERR 2 slot out of range`)
- `LIST` 응답: 줄 단위 `SLOT <n> <TXT|IMG> <미리보기>` 반복 후 `OK LIST`

```
SLOT 1 TXT 사랑해
SLOT 2 IMG 24col
SLOT 3 TXT rainbow:공주님
OK LIST
```

### 오류 코드

| 코드 | 의미 |
|------|------|
| 1 | 알 수 없는 명령 |
| 2 | 인자 범위 초과 (슬롯 번호 등) |
| 3 | 저장 공간 부족 |
| 4 | CRC 불일치 |
| 5 | 이미지 청크 순번 오류 |
| 6 | 텍스트에 폰트 없는 문자 포함 (해당 문자는 □로 대체 후 경고) |

## 5. 색 지정

| 형식 | 예 | 의미 |
|------|-----|------|
| `RRGGBB` | `FF66CC` | 단색 (hex) |
| `rainbow` | | 글자/컬럼 진행에 따라 무지개 순환 |
| `grad:RRGGBB-RRGGBB` | `grad:FF0000-0000FF` | 시작→끝 그라데이션 |

## 6. 이미지 전송 예시 (8×24 하트, 576바이트)

```
앱 → IMGB 5 24
기기 → OK IMGB
앱 → IMGD 0 <base64 180바이트 분량>
앱 → IMGD 1 <base64 180바이트 분량>
앱 → IMGD 2 <base64 180바이트 분량>
앱 → IMGD 3 <base64 나머지>
기기 → (청크마다 응답 없음 — 흐름 단순화)
앱 → IMGE 8F3A21BC
기기 → OK IMGE        ← 이 시점에 플래시 기록 완료
```

## 7. 이벤트 (기기 → 앱, 선택 구현)

연결 중일 때만 push. 앱은 없어도 동작에 지장 없음.

| 이벤트 | 의미 |
|--------|------|
| `EV SWING <주기ms>` | 스윙 감지됨 (앱에서 "흔드는 중" 표시용) |
| `EV SLEEP` / `EV WAKE` | 슬립 진입/기상 |
| `EV BAT <퍼센트>` | 배터리 변화 (10% 단위) |

## 부록: 펌웨어 버전 규칙

`INFO`의 `fw=` 값(semver)으로 앱이 UI를 전환한다:
- **1.0.0 미만** = 단색(GREEN) 완드 → 앱이 색 선택을 숨기고 초록 미리보기로 전환
- **1.0.0 이상** = RGB 완드 → 풀컬러 UI
- 버전 판별 불가(시뮬레이터 `fw=sim` 등) → 풀컬러 UI

현재: 선물용 GREEN 완드 펌웨어 = 0.0.1. OTA(BLE DFU) 서비스 내장 — nRF Connect 앱에서 빌드 zip으로 무선 업데이트 가능.

## 부록: 주인 지정

펌웨어의 `OWNERS[]` 테이블에 보드 MAC 주소(부팅 시 시리얼 모니터에 출력)를
등록하면 그 보드의 주인 이름·시리얼이 정해진다. 등록된 보드는 부팅 크레딧으로
`<이름>의 선물 <시리얼>`을 표시하고, `INFO` 응답에 `owner=`/`serial=`/`mac=`이 포함된다.
이름에는 공백을 넣지 않는다 (INFO가 공백 구분이므로).

## 변경 이력

- v0.1 (2026-07-29): 최초 작성. INFO에 owner/serial/mac 필드 추가
